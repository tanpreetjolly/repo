import Carousel from '@/components/Carousel/Carousel';
import { Wrapper } from '@/components/Layout';
const Hero = () => {
  return (
    <Wrapper>
      <Carousel />
    </Wrapper>
  );
};

export default Hero;
